from setuptools import setup

setup(
   name='sub',
   version='1.0',
   description='Sezzle module',
   author='Shrivardhan',
   author_email='bhara054@umn.edu',
   packages=[],
   install_requires=['Flask>=0.10.1', 'flask-cors'], #external packages as dependencies
)
